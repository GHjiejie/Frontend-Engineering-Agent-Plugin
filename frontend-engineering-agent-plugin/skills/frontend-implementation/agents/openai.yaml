interface:
  display_name: "Frontend Patch Implementation"
  short_description: "Propose, approve, apply, and verify frontend patches"
  default_prompt: "Use $frontend-implementation to prepare a reviewable patch for this approved plan."
