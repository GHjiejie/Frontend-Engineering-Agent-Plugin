interface:
  display_name: "Generate Frontend Spec"
  short_description: "Create or resume an isolated frontend specification"
  default_prompt: "Use $generate-frontend-spec, collect my PRD, prototype, and API inputs, then ask whether to resume an existing feature or create an isolated new one."
