interface:
  display_name: "Generate Frontend Spec"
  short_description: "Collect inputs, review interactions, and generate a frontend specification"
  default_prompt: "Use $generate-frontend-spec for this frontend feature, ask me for missing PRD, prototype, and API inputs, and wait for my interaction approval before finalizing."
