interface:
  display_name: "User Flow Generator"
  short_description: "Generate versioned flows from confirmed product decisions."
  default_prompt: "Use $user-flow-generator to generate the user flow in the confirmed frontend project and feature version."
