interface:
  display_name: "User Flow Generator"
  short_description: "Generate user flows from clarified product decisions."
  default_prompt: "Use $user-flow-generator to generate a Mermaid user flow after the clarification gate passes."
