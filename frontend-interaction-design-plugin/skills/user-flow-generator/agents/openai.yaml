interface:
  display_name: "User Flow Generator"
  short_description: "Map PRDs and prototypes into reviewable user flows."
  default_prompt: "Use $user-flow-generator to turn this PRD and prototype into a Mermaid user flow."
