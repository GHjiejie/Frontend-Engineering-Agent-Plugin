interface:
  display_name: "User Flow Generator"
  short_description: "Map evidence-backed user goals and visible outcomes."
  default_prompt: "Use $user-flow-generator to generate versioned user flows from passed evidence and confirmed decisions."
