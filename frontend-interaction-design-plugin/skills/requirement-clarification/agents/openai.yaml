interface:
  display_name: "Requirement Clarification"
  short_description: "Resolve consequential conflicts across durable sources."
  default_prompt: "Use $requirement-clarification to compare the persisted PRD, prototype evidence, and API contract and record required decisions."
