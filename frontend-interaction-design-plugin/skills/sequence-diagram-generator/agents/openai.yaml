interface:
  display_name: "Sequence Diagram Generator"
  short_description: "Map approved UI behavior to confirmed API sequences."
  default_prompt: "Use $sequence-diagram-generator to map approved flows and states to the confirmed API contract."
