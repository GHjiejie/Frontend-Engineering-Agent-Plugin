interface:
  display_name: "Sequence Diagram Generator"
  short_description: "Map confirmed versioned UI behavior to API sequences."
  default_prompt: "Use $sequence-diagram-generator to map the confirmed feature flows, states, and APIs into sequence diagrams."
