interface:
  display_name: "Frontend Plan Generator"
  short_description: "Assemble clarified interaction artifacts into a delivery plan."
  default_prompt: "Use $frontend-plan-generator to create the final plan only after the clarification gate passes."
